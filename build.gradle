plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    // Add JUnit 5 for testing
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
}

test {
    useJUnitPlatform() // Ensure JUnit 5 tests run
}
